def main():
    packageSizes = getPackageSizes();
    totalSize = getTotalSize();

    for packageSize in packageSizes:
        if(packageSize is a multiple of totalSize):
            We have found a solution!
            
            numberOfPackages = totalSize // packageSize
            packageSize = the size of each package.

            We can now quit the program since we have found a solution.


    if we reach here, we haven't found a solution!

def getPackageSizes():
    pass;

def getTotalSize():
    pass;